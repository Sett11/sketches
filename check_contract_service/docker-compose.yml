version: '3.8'

services:
  check-contract:
    build:
      context: .
      dockerfile: Dockerfile.check_contract
    container_name: check-contract
    ports:
      - "${CHECK_CONTRACT_PORT:-8002}:8002"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    env_file:
      - .env